/* The only backend Gzowo Labs has: a counter.
   Public by design — the rules on this database allow reading the counts and
   adding exactly one to a count, and nothing else. */
window.GZOWO = {
  rtdb: 'https://jerzysukiennik-hub-default-rtdb.firebaseio.com'
};
